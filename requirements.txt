line-bot-sdk==2.3.1  # 支持最新功能和修復
flask==2.2.5  # 與 Werkzeug 更好兼容
requests==2.31.0  # 最新版本，提升性能與安全性
Werkzeug==2.3.7  # 與 Flask 最新版本兼容
boto3==1.28.65  # 支持最新的 AWS 功能
psycopg2-binary==2.9.9  # PostgreSQL 驅動
pgvector==0.2.4  # 向量數據支持
langchain==0.0.354  # 保持指定版本（LangChain 的更新可能破壞接口）
chromadb==0.3.29  # 與 LangChain 相容的版本
openai==1.3.8  # OpenAI 最新穩定版
opencc-python-reimplemented==0.1.7  # 中文繁簡轉換工具
# Development dependencies (可選)
pytest==7.4.0  # 單元測試框架
flake8==6.1.0  # 代碼風格檢查
